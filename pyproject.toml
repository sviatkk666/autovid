[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
# Errors + real bugs only; style is left to the codebase's own conventions.
select = ["E9", "F"]
ignore = ["F841"]
